import React, { useState, useEffect } from "react";
import { StyleSheet, Text, View } from "react-native";

export default function DonatorMainScreen(){
    return(
        <Text style = {styles.title}>Te</Text>
    )
}
const styles = StyleSheet.create({
    title: {
        fontSize: 15,
      },
})